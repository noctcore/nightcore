{
  "name": "@nightcore/desktop",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "description": "Nightcore desktop shell — Tauri 2 + Rust core (orchestration) over the Bun provider sidecar.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml",
    "check": "cargo check --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  }
}
