# cargo-audit config for the Nightcore Rust core. Read by `cargo audit` when it
# runs from apps/desktop/src-tauri (the Dependency Audit CI job does).
#
# Ignores are for advisories with NO upstream fix path we control — each entry
# must say where the crate comes from and when to remove it. Never ignore an
# advisory on a direct dependency we can bump ourselves.

[advisories]
# Empty as of the 2026-08-31 dependency refresh
# (docs/deps/2026-08-31-dependency-upgrade-plan.md, Phase 1): `cargo update`
# collapsed both locked quick-xml instances (0.39.4 via plist → tauri,
# 0.37.5 via tauri-winrt-notification → notify-rust → tauri-plugin-notification)
# into a single quick-xml 0.41.0, clearing RUSTSEC-2026-0194 and
# RUSTSEC-2026-0195. See git history for the prior ignore entries.
ignore = []
