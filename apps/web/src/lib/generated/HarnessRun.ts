// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { HarnessUsage } from "./HarnessUsage";
import type { StoredConventionFinding } from "./StoredConventionFinding";
import type { StoredHarnessProposal } from "./StoredHarnessProposal";
import type { StoredProposedArtifact } from "./StoredProposedArtifact";
import type { StoredRepoProfile } from "./StoredRepoProfile";
import type { StoredRuleCoverageGap } from "./StoredRuleCoverageGap";

/**
 * One Harness scan, persisted under `.nightcore/harness/<id>.json`.
 */
export type HarnessRun = { id: string, projectPath: string, 
/**
 * `running` | `completed` | `failed`.
 */
status: string, 
/**
 * The convention lenses requested for this scan (wire strings).
 */
categories: Array<string>, model: string, createdAt: number, updatedAt: number, costUsd: number, durationMs: number, usage: HarnessUsage, profile: StoredRepoProfile, findings: Array<StoredConventionFinding>, artifacts: Array<StoredProposedArtifact>, 
/**
 * The task-shaped proposals synthesis produced (the unit the user converts to a
 * board task). Additive (`#[serde(default)]`) so a pre-proposals on-disk scan loads
 * with an empty set.
 */
proposals: Array<StoredHarnessProposal>, 
/**
 * ENFORCE-lite rule coverage: one record per convention (`enforced` /
 * `documented-only` / `unenforced`). Additive (`#[serde(default)]`) so a
 * pre-coverage on-disk scan loads with an empty set — no migration, coverage,
 * not conformance.
 */
coverage: Array<StoredRuleCoverageGap>, 
/**
 * Set while the serial synthesis pass runs (after every lens, before the
 * terminal event) so a run reloaded mid-synthesis still projects the
 * "Synthesizing…" state instead of the all-lenses-done dead zone.
 */
synthesizing: boolean, error: string | null, };
