// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { GauntletTotals } from "./GauntletTotals";
import type { GuardrailTotals } from "./GuardrailTotals";
import type { JournalSummary } from "./JournalSummary";
import type { MergeTotals } from "./MergeTotals";
import type { SpendTotals } from "./SpendTotals";
import type { TrustBadge } from "./TrustBadge";

/**
 * The whole repo-scoped governance posture: what the gates verdicted across every
 * task, what the rails stopped, what it cost, and the human decisions behind it.
 */
export type ProjectTrustSummary = { 
/**
 * ISO-8601 UTC mint time — the summary is computed on demand, so this is when
 * the numbers were true, not when anything was saved.
 */
generatedAt: string, merges: MergeTotals, gauntlet: GauntletTotals, guardrails: GuardrailTotals, spend: SpendTotals, journal: JournalSummary, 
/**
 * The shields-compatible badge derived from the totals above — the same value
 * the badge export serializes, so the dashboard and the published badge can
 * never disagree.
 */
badge: TrustBadge, };
