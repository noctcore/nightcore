// StaleChip is a pure presentational chip — no local state, effects, or queries. This
// module exists to satisfy the folder-per-component sibling contract and is the home for
// any future state.
export {};
