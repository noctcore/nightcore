/** The Terminal feature barrel — the global tabbed user-shell view (decision 4).
 *  The shell lazy-loads `TerminalView` through this barrel. */
export { TerminalView } from './TerminalView';
export type { TerminalViewProps } from './TerminalView/TerminalView.types';
