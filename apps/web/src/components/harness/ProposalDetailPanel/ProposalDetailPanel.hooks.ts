// ProposalDetailPanel is a pure presentational sheet with no local state, effects, or
// queries — its lifecycle actions are handlers passed in from HarnessView, and the
// `pending` flag is owned upstream. This module exists to satisfy the
// folder-per-component sibling contract and is the home for any future state.
export {};
