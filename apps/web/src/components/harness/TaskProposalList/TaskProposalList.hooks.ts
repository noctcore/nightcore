// TaskProposalList is a pure presentational panel with no local state, effects, or
// queries — the proposals, loading flag, and empty message are derived upstream in the
// HarnessView hook. This module exists to satisfy the folder-per-component sibling
// contract and is the home for any future state this component grows (e.g. the
// convert / dismiss / go-to-task actions that land with the detail panel).
export {};
