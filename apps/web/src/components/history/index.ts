/** Public entry point for the History feature: the global cross-family run
 *  History view, its merge hook, and the summary types the shell wires against. */
export type { AllScanRuns, ScanFamily, ScanRunSummary } from './HistoryView';
export { HistoryView } from './HistoryView';
export { useAllScanRuns } from './HistoryView';
