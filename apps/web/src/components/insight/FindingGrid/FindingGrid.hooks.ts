// FindingGrid is a pure presentational grid with no local state, effects, or
// queries — the findings, skeleton count, and empty message are derived upstream
// in the InsightView hook. This module exists to satisfy the folder-per-component
// sibling contract and is the home for any future state this component grows.
export {};
