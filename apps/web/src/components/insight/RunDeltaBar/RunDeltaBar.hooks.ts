// RunDeltaBar is a pure presentational strip with no local state, effects, or
// queries — the delta itself is computed upstream by `computeInsightRunDelta` in the
// InsightView hook (which owns the persisted run list). This module exists to
// satisfy the folder-per-component sibling contract and is the home for any future
// state this component grows.
export {};
