// CategoryTabs is a pure presentational tab strip with no local state, effects,
// or queries — its tab descriptors are derived upstream in the InsightView hook.
// This module exists to satisfy the folder-per-component sibling contract and is
// the home for any future state this component grows.
export {};
