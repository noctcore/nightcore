// SeatCanvas is a pure presentational binding — it renders the seat nodes the parent
// folded from the transcript, with no local state, effects, or queries. This module
// exists to satisfy the folder-per-component sibling contract and is the home for any
// future state the canvas grows (e.g. per-seat expand/collapse).
export {};
