// TeamChat is a pure presentational binding — it renders the folded chat projection
// with no local state, effects, or queries. This module exists to satisfy the
// folder-per-component sibling contract and is the home for any future state (e.g. a
// kind filter or a jump-to-latest control).
export {};
