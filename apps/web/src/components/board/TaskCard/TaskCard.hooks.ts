// TaskCard is a pure presentational card with no local state, effects, or
// queries — selection is owned by the board. This module exists to satisfy the
// folder-per-component sibling contract.
export {};
