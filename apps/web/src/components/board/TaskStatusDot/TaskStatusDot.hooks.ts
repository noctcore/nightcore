// TaskStatusDot is a pure presentational binding with no local state, effects,
// or queries — all status→class mapping lives in the feature-root `status.ts`.
// This module exists to satisfy the folder-per-component sibling contract and
// is the home for any future state this component grows.
export {};
