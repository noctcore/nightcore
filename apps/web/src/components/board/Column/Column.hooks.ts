// Column is a pure presentational layout with no local state, effects, or
// queries — task grouping is computed by the parent Board. This module exists
// to satisfy the folder-per-component sibling contract.
export {};
