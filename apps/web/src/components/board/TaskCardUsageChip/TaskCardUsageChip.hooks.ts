// TaskCardUsageChip is a pure presentational leaf — it takes the derived hot window
// as a prop (from the card, which reads it via `useUsageHot`), so it owns no state.
// This module satisfies the folder-per-component sibling contract and is the home for
// any future state this chip grows.
export {};
