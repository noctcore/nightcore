// TaskCardUsageChip takes no props — it reads the derived hot window from the shared
// `UsageHotContext` (via `useUsageHot`), like `TaskCardTerminalChip` reads its own
// linked-terminal state. This module satisfies the folder-per-component sibling
// contract and is the home for any prop types this chip grows.
export {};
