# Kirei Wave — Council (send-input → P1 → P2)

**Canonical tracker = GitHub #334** (this file is scratch; it has been wiped by branch switches — trust #334 + project memory).

## ✅ P1 MVP COUNCIL — COMPLETE (2026-07-13/14)
9 PRs merged, each 3 green lights (CI + attribution + kirei-gate MERGE):
- Phase 0 send-input #335→**#347**; hardening→**#355**; foundation #348→**#356**; presets #349→**#357**; conductor #350→**#358**; collector #351→**#359**; canvas #352→**#360**; converge gavel #353→**#362**; safety capstone #354→**#363**.
- All 8 P1 issues CLOSED. 7 safety non-negotiables CI-enforced (`packages/engine/src/debate/council-safety.test.ts`).

## ✅ Live-testing bug — #364 FIXED via PR #373 (merged 2026-07-14)
Council seats bypassed the Rust pending-launch FIFO (launched engine-internally) → `correlation desync` warn flood + dropped seat `nc:session` + mis-binding hazard. Fix = `council:true` marker (CouncilRouter→SessionManager→reader) + reader carve-out (skip FIFO for council seats); board/scan/pr-fix byte-identical. gate MERGE (pure carve-out verified, mis-bind regression test). Live-confirmed GOOD: per-seat sandbox active, opus+sonnet seats spawn. Residual → **#374** (preflight-refused seat edge, MEDIUM, pre-existing/unreachable-at-plan-tier).

## ▶️ P2 — Objective Council: STARTED 2026-07-17 (user resumed "run the P2 slices")
3-wave dependency-ordered build (worktree-isolated builders, each gated CI+attribution+kirei-gate before merge):
- **Wave 1:** ✅ BOTH MERGED. #365 objective-gate FOUNDATION → **PR #375** (kirei-gate: non-bypassable override; 2 LOW → #370/#366). #371 editable canvas edges → **PR #376** (rebased onto #375 — clean, conductor.ts=389<400, verified local green then CI; kirei-gate: routing subtract-only + conductor-mediated; 1 LOW → filed follow-up).
- **Wave 2 (on merged #365, branched from 54dd9a30 pre-#376 → each needs rebase):**
  - #372 no-progress → **PR #378 MERGED** (rebased, hand-resolved conductor.ts+debate-round.ts, browser-cov ENOSPC flake re-run, kirei-gate MERGE).
  - #366 single-writer Build (SECURITY-CRITICAL) → **PR #379 MERGED** (rebased onto #375+#376+#378, hand-resolved 3 conductor.ts conflicts + this.dispatchConfig→stageDispatchConfig fix + import-sort; strictest kirei-gate MERGE — all 8 Build-security points verified; browser-cov clean). 2 watch-points threaded to #367/#368 (real driver + scan-and-record). conductor-propose/build/build-writer extracted; Build DORMANT.
  - #370 judge/vote → **PR #380**: rebased onto #375+#376+#378+#379, HAND-RESOLVED 6 conductor.ts conflicts. KEY: #380's conductor-plan.ts DUPLICATED main's conductor-dispatch.ts (planDispatch≡stageDispatchConfig) → DELETED conductor-plan.ts, adopted stageDispatchConfig everywhere (Propose/Debate/autoconverge over `debaters`); removed #380's re-added propose+dispatchConfig methods. tsc CLEAN. BUT conductor.ts=426>400 ratchet → DELEGATED builder (a83b4c8b) to extract a helper + re-verify + force-push. Autoconverge + partition + overrideGate re-key intact. FOLLOW-UP: registry preset selecting judge-agent/vote needs CouncilPresetId+Rust codegen (scoped out).
  - #370 judge/vote → **PR #380 MERGED** (6-region hand-resolve consolidating conductor-plan→conductor-dispatch + builder extracted drive→conductor-drive.ts to fit 400; kirei-gate MERGE — gate-outranks-judge/vote, judge-scanned, human-terminal all verified). 2 LOW: runBuild-over-seats-vs-debaters threaded to #367/#368; votePrompt docstring nit (noted, trivial).
- **Wave 2 COMPLETE (5/8).** main @ fcf73bd3 (#365+#371+#372+#366+#370).
- **Wave 3 (IN FLIGHT — 1/3 done):**
  - #369 adversarial Review → **PR #381 MERGED** (exec-neutral ReviewDriver seam; Build→Review→Converge; gate overrides Review; verdict scanned+fenced+mediated; dormant triple-gate; chargeSpend budget-only-tightens; kirei-gate MERGE). LOW (coerce-to-enum when real reviewer wired) threaded to #367/#368.
  - #367 UI-bug preset → **PR #382 MERGED** (repro red→green gate; rebased onto #381, keep-both test suites; DEFERRED real driver→#383; shared `objective-preset.ts`; write dormant honest; debaters-only writer; integration fix #381 Review presets +objectiveGate='repro' behavior-preserving; kirei-gate MERGE — deferral honest, gate overrides consensus, both suites intact).
  - #368 Coding preset → **PR #384 MERGED** (debate-the-plan; reused objective-preset.ts + `build` gate kind; validator invariant generalized behavior-preserving; honest deferral→#383; kirei-gate MERGE — all 5 cruxes).
- **✅✅ P2 COMPLETE 2026-07-17 — ALL 8 SLICES MERGED + CLOSED (#365-372).** 8 PRs #375/#376/#378/#379/#380/#381/#382/#384. main @ 2da7bd9d. Summary posted to #334.
- **#383 IN FLIGHT 2026-07-17 (user chose "build #383 now").** Real write-capable SessionBuildDriver = autonomous code execution.
  - DESIGN PASS DONE (Explore agent). Design saved `scratchpad/383-design.md`. HEADLINE: every confinement guarantee already exists+reused by board tasks; the ONLY new surface = a path-less, councilRunId-keyed engine→host RPC (modeled on the parked-permission seam reader.rs:286) so the in-engine Council reaches Rust `crate::worktree` ops. Host DERIVES worktree path from councilRunId, NEVER trusts engine-sent path → adds a message type, not a write/exec sink. Reuse: worktree allocate/merge/remove, PreToolUse evaluateWorkspaceConfinement (auto-scopes to writer cwd), platform::git_command, TaskLease, prepareWriteSandbox, SessionSeatDriver+BUILD_WRITER_HARDENING, gauntletObjectiveGate. Driver=engine-side TS session-build-driver.ts injected at council-router.ts:76. Merge/discard=HUMAN (existing merge_task), driver never auto-merges.
  - **#383 → PR #386 MERGED 2026-07-17.** Council now ACTUALLY WRITES CODE. STRICTEST GATE PASSED: 3 parallel adversarial kirei-gates (RPC path-injection/host-trust · session write-path/sandbox · gate-override/single-writer/composition) ALL VERDICT:MERGE + CI green + attribution clean. Verified: path-less RPC, host-derived path from webview-trusted UUID registry (engine can't forge/register), confinement reused verbatim (writer cwd=worktree fails-closed, Seatbelt roots=worktree, auto-accept-not-bypass, one-writer-from-debaters), gate un-forgeable (manifest-trust split), RED overrides consensus, merge=human. ⚠ CI-exec-neutral (runWriter faked) → REAL live end-to-end UNVERIFIED, needs MANUAL DOGFOOD (ui-bug/coding council on repo w/ .nightcore/harness.json). Hardening→#387.
- **COUNCIL FULLY BUILT: P1 + P2 (8 slices) + #383 real driver.** main @ f271c2a6. Open follow-ups (all non-blocking): #387 (driver hardening), #377/#385 (nits), #374/#361 (P1 residuals). #334 tracker.
- Open follow-ups: #383 (real driver), #377 (RoutingPolicy seed), #385 (assertNever fail-open guard), #374 (preflight-refused-seat edge), #361 (mediated broadcast/DM/steer). All filed, none blocking.

## ▶️ COUNCIL HARDENING BATCH — 2026-07-17 (user: "clear leftovers then take v0.4 tasks while away")
Clearing the small Council follow-ups first, each gated CI+attribution+kirei-gate. Solo/sequential = branches off main (no worktrees).
- **#388 MERGED (f2d2e673)** — engine defense-in-depth: #385 `default: assertNever` fail-open guard on objectiveGateForPreset + #377 RoutingPolicy seeds via shared `validatedEdges` (same validSeatIds filter as live edits) + #387 empty-string worktreePath guard + stale-docstring refresh (driver shipped, not dormant). Closes #385+#377. gate MERGE (flagged 1 pre-existing LOW: council-routing.ts `normalizeEdges` uses NUL `\x00` dedup delimiter → file diffs as BINARY, hides changes; fail-safe, not mine → FOLLOW-UP to switch to text-safe delimiter).
- **#389 MERGED (2d959628)** — Rust run-id validation (part of #387): `is_filesystem_safe_run_id` (`[A-Za-z0-9-]`, non-empty, ≤128) enforced at BOTH register (skip+warn) + build_project_path (hard gate before worktree_path). gate MERGE (re-ran tests; byte-level charset closes every escape incl Unicode/overlong).
- **#390 MERGED (e68fe43d)** — preflight-refused seat carve-out (Closes #374). Refused seat emits only session-failed (no session-started marker) → reader correlated it → could pop a concurrent board task's slot. Fix: engine echoes `council` marker onto refused session-failed (new optional SessionFailedEvent.council, serde-additive, Rust codegen regen) + refusal mapping EXTRACTED to session-preflight-refusal.ts (session-manager.ts was AT 400 cap → extract like session-start-params) + reader skips ANY council-marked event (event_is_council_marked); board refusal (no marker) still correlates. gate MERGE (board-failure path intact, extraction verbatim, marker un-spoofable, forget=no-op).
- **#391 MERGED (87642304)** — text-safe dedup key (from #388 gate LOW): council-routing.ts normalizeEdges NUL→JSON.stringify([from,to]); file now diffs as TEXT (was binary, hid the security file's diffs). Behavior-preserving (transient Map key). gate MERGE.

## ✅ COUNCIL HARDENING BATCH COMPLETE — main @ 87642304. 4 PRs (#388/#389/#390/#391), all 3-gated. Cleared: #385 #377 #374 + #387(host runid + empty-string + docstring) + NUL fix.

## Open follow-ups (all filed)
#387 (REMAINING deferred, commented on issue: fail-CLOSED Seatbelt for writer, dep-provisioning, rate cap, confused-deputy — need cross-platform thought) · #361 (mediated broadcast/DM/steer — big feature) · #334 (parent tracker).

## ▶️ v0.4 task #158 (Skill registry stages 0–1 + contract debt) — user delegated "any v0.4 task while away".
Picked #158 = safest autonomous. Scoping agent (aea7568) mapped all 5 sub-items + found LATENT WIRE BUG: generated.rs TaskKind `rename_all="lowercase"` vs task_kind.rs `snake_case` — identical for today's single-word kinds but a future multi-word kind (`ui_bug`→"uibug" vs "ui_bug") diverges. Part of the collapse (deferred).
- **✅ Slice A: codegen:check meta-target → PR #392 MERGED (27ec8472).** scripts/codegen-check.ts aggregates zod→Rust drift + TS canaries/conformance + Rust ts-rs regen + parity + web-binding git-diff. Additive tooling, no product code. gate MERGE (verified all legs fail-loud on drift, no false-pass).
- **▶️ Slice B: engine builtin-kinds-as-data → PR pending.** resolveKindPreset switch→Record<TaskKind,KindPreset> (KEEPS compile-exhaustiveness — TS Record over finite union) + builtinKindKeys() + runtime parity test (keys==TaskKindSchema.options). Behavior-preserving (consumers read-only), all per-kind security rationale carried inline. Committed 3851343e, rebased, pushed. → PR+gate.
- **DEFERRED to user (design trade-offs / invasive — NOT safe to decide while away):**
  - capabilities-over-wire (delete web CLAUDE_CAPABILITIES): the hand-copy is DELIBERATELY kept as the synchronous Claude fail-open default (its own docblock) — deleting = transient-null cold-start UX for Claude (like Codex). Real UX trade-off. A parity-test middle-ground can't import engine across the web layer boundary. → user decides delete-vs-keep.
  - TaskKind triple-authoring collapse: 7 sites (contracts/config.ts zod + codegen ENUM_NAMES + task_kind.rs enum+as_wire + mod.rs parity arrays + workflow/kind.rs + engine kind-presets + web KIND_LABEL/kindIcon). Invasive; also must single-source the lowercase-vs-snake_case rename. → user.
  - Rust kind.rs match→HashMap: would trade compiler-exhaustiveness for a parity test (safety DOWNGRADE for builtins). → user judgment.
  - SkillDescriptorSchema: new contract surface, shape needs decisions (mirror council-preset id-enum vs ProviderCapabilities object). Land last (consumes the data tables). → user.
NOT picked: #157 (default-on staging=product call), #197 (calibration needs #150 real-run data), #279 (dep #194), #169 (Windows).

## Merge log (this arc): P1/P2/#383 = #347 #355 #356 #357 #358 #359 #360 #362 #363 #373 #375 #376 #378 #379 #380 #381 #382 #384 #386. Hardening = #388 #389 #390 #391. All squash-merged, worktrees reclaimed.
