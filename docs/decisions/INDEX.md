# Nightcore Decision Register

The single canonical list of architectural decisions and their status. When a decision is reversed, set the old row to **superseded** and link the doc that supersedes it IN THE SAME CHANGE — never leave contradictory prose in living docs (`AGENTS.md`, `docs/architecture.md`, code comments). Each new decision also gets a dated `docs/<category>/YYYY-MM-DD-<slug>.md` with the standard header (Date / Lens / Scope / Baseline).

Status legend: ✅ active · ⛔ superseded

| ID | Date | Decision | Status | Source / Supersedes |
|----|------|----------|--------|---------------------|
| D-001 | 2026-06-23 | The user's installed `claude` binary is a required prereq; it is NOT bundled. | ✅ active | docs/arch |
| D-002 | 2026-06-24 | Remove `@nightcore/tools` + `@nightcore/mcp`; rely on Claude's native tools + permission model. MCP is UI-configurable via the SDK `Options.mcpServers`, not in-code. | ✅ active | supersedes the earlier "parked seam, don't delete" |
| D-003 | 2026-06-21 | The Claude Agent SDK (runtime and types) is confined to the provider boundary directory `packages/engine/src/providers/claude`; everything outside talks the neutral `AgentProvider` seam / contract types, and a new package needing the model routes through the `@nightcore/engine` façade. | ✅ active | refined by issue #18 (provider abstraction); enforced by lint (`no-restricted-imports`, `layer-rank`) |
| D-004 | 2026-06-27 | Rust core god-file decomposition: `m2/` renamed to `orchestration/`, Tauri handlers lifted to `commands/` (the store becomes a pure leaf), and the Insight/Harness/Scorecard trio unified behind a generic `RunStore<T>`. | ✅ active | docs/research/2026-06-27-rust-core-architecture-decomposition.md |
| D-005 | 2026-06-30 | Worktree overhaul: task-scoped worktrees (`nc/<taskId>` branches) with a branch picker plus a standalone manager; merges stay safe abort-not-force with a read-only preview, no AI auto-merge. | ✅ active | Worktree overhaul shipped; enforced in the worktree + orchestration code. |
| D-006 | 2026-06-26 | Open the `TaskKind` enum into a skill/agent registry (Build · Research · TDD · Decompose) as the control-panel keystone; governance is a tiered sandbox with a Core-skills-first posture. | ✅ active | Control-panel keystone shipped; TaskKind opened to Build · Research · TDD · Decompose. |
| D-007 | 2026-07-02 | Ship the full PR system (create · status/finalize/push · address-comments · AI PR-reviewer scan sibling); the reviewer is diff-centric and read-only, posting one atomic human-gated `gh api` review. | ✅ active | docs/research/2026-07-02-pr-system-design.md |
| D-008 | 2026-07-02 | Hardening/scan enforcement tiers: 18 machine-checkable gate modules with deny/ask/allow runtime tiers, a flight-recorder ledger, prompt-injection quarantine, and an opt-in write sandbox, surfaced in a Policy tab. | ✅ active — **except its "opt-in write sandbox" clause, superseded by D-012** | docs/research/2026-07-02-hardening-module-catalog.md; sandbox clause superseded by D-012 |
| D-009 | 2026-07-07 | Codex runs as an engine-side provider inside the existing Bun sidecar; no provider-specific sidecar binary or packaging matrix is added. | ✅ active | docs/arch/2026-07-07-codex-single-sidecar-provider.md; supersedes the earlier "every provider is a separate sidecar binary" design prose |
| D-010 | 2026-07-30 | Terminal renderer default is platform-resolved: xterm WebGL on by default on Windows/Linux, DOM on macOS while xtermjs#5816 (WebGL corruption, repro'd from Tauri, re-confirmed on macOS 15.7) is open. `Settings.terminalWebglEnabled` is tri-state — `null` means "resolve in the surface", so an explicit user opt-out is never flipped by a default change. ***Amended 2026-07-31 (#430) — macOS stays DOM, and the flip-back condition is sharpened:*** the fix, xtermjs#5883, **merged upstream 2026-05-21** but is in **no stable release**. It changes only `addons/addon-webgl/src`, so it ships in `@xterm/addon-webgl` (not the `@xterm/xterm` core), whose newest stable version is still `0.19.0` (2025-12-22) — the version this repo takes — and the first published artifact carrying it is the `0.20.0-beta.219` **prerelease**, which peer-requires a beta core. Verified by tarball inspection: the fix's markers (`AtlasPage.nextVersion`, `invalidateAtlasTextures`, `MERGE_RETRY_LIMIT`) are absent from `0.19.0` and from `0.20.0-beta.218`, present from `0.20.0-beta.219` on. **The trigger is therefore a STABLE `@xterm/addon-webgl` ≥ 0.20.0 carrying #5883 — not #5816 being closed** (an issue can outlive its fix; #5816 is still open). Flipping on a prerelease is not in scope: xterm 6 removed the canvas renderer, so a corrupted GPU context has only DOM left, and the failure is visual — no gate here can catch it. | ✅ active | docs/research/2026-07-10-terminal-build-spec.md (amends its decision 7, "DOM default everywhere"); resolution in apps/web/src/lib/platform.ts; 2026-07-31 re-verification in #430 |

| D-011 | 2026-07-30 | Per-project governance history is an APPEND-ONLY journal (one NDJSON record per quarantine / policy-save / arm / disarm / ratchet, written `O_APPEND` + one bounded write, never rewritten or compacted), and the repo-scoped trust summary over it is COMPUTED ON DEMAND, never persisted — a cached summary would be a second source of truth free to drift from the journal. The journal lives inside the store dir Nightcore already owns and is kept out of the user's `git status` by a self-ignoring nested `.gitignore` rather than by editing their root ignore file. | ✅ active | apps/desktop/src-tauri/src/store/governance.rs; apps/desktop/src-tauri/src/workflow/project_trust/mod.rs |

| D-012 | 2026-07-12 | **Native-sandbox adoption + staged default-on** (T12's D4 + D3, executed as T16 / #157). **D4 — ADOPT (hybrid):** delete the custom macOS Seatbelt profile writer and emit the Agent SDK's native `Options.sandbox`; **KEEP every PreToolUse policy gate** — the native sandbox isolates Bash subprocesses only, so it can never confine `Write`/`Edit`/`NotebookEdit`/`ApplyPatch`/`mcp__*`, and the two layers cover disjoint surfaces. Credentials adopt `deny` mode now; `mask` is version-gated and parked behind three re-verifiable prerequisites. **D3 — staged default-on:** phase 1 = ON by default for **macOS + worktree-mode** runs, with a durable global opt-out (Settings → Permissions, tri-state Auto/Always/Never) AND a per-run override, `failIfUnavailable: false` plus a LOUD "containment unavailable" surface (log + flight-recorder marker + activity-log badge), and local telemetry before widening. Phase 2 (Linux) is CANCELLED — Linux is out of project scope. Phase 3 (main-mode + fail-closed) awaits clean telemetry and is not implemented. | ✅ active | Recommendation: docs/research/2026-07-12-platform-primitives-spike.md §1/§1b/§6/§7. User's answers: D4 on issue #153 (2026-07-12), D3 in the 2026-07-12 grill (+ the same-session "no Linux distro support for now" amendment). Supersedes D-008's "opt-in write sandbox" clause. Implementation: `packages/engine/src/providers/claude/native-sandbox.ts`, `Settings::sandbox_writes_for`. |

Add a new row for every decision; annotate or update any superseded prose in living docs in the same change that records the reversal.
